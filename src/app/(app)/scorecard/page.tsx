import { Metadata } from "next";

export const metadata: Metadata = {
    title: "ScoreCard",
    description: "Record live stats for a MUFL Ultimate game as it happens",
};

export default function ScoreCard() {
    return <div></div>;
}
